pip>=24.2
setuptools>=75
wheel>=0.44

# Core numeric/ML
numpy>=1.26,<2.1
pandas>=2.1,<3
scipy>=1.11,<2
scikit-learn>=1.3,<2
optuna==4.0.0
# numba needs numpy<2.1; loosen but keep compatible range
numba>=0.59,<0.61

# PyTorch (choose CUDA build at install time)
# Example: pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio
torch>=2.4,<3
torchvision>=0.19,<0.21
torchaudio>=2.4,<3

# Ray + RLlib (supports Linux py3.13)
ray[default,rllib]==2.53.0

# Gymnasium + SB3 (install SB3 from git for py3.13 until wheels are available)
gymnasium[box2d,atari]>=0.29,<0.30
stable-baselines3 @ git+https://github.com/DLR-RM/stable-baselines3.git

# Tree/boosting models
lightgbm>=4.3,<5
xgboost>=2.0,<3
catboost>=1.2,<2

# Deep models
pytorch-tabnet>=4.1,<5
timm>=1.0,<2

# Optional GPU TA calc (install matching CUDA build)
# Example: pip install cupy-cuda12x==13.6.0
cupy-cuda12x>=13,<14

# Misc
pyarrow>=14,<18
psutil>=5,<7
tensorboard>=2.16,<3
matplotlib>=3.8,<4
tabulate>=0.9,<1
